use html5;

// The loader should find 'MyModule' in the 'module/' directory
[Import] @Chtl from "MyModule";

body
{
    h1 { text: "Module Test"; }

    // This should now be resolved from the imported module
    @Element MyCard from MyModule;
}
