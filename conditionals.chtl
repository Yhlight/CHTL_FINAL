html {
    body {
        div {
            style {
                width: 150px;
                background-color: width > 100px ? red : blue;
                padding: width < 200px ? 10px : 20px;
                margin-top: width > 200px ? 0 : -10px;
            }
        }
    }
}
