html {
    head {
        title { text { "Origin Test" } }
    }
    body {
        h1 { text { "Content from CHTL" } }

        [Origin] @Html {
            <div class="raw-html">
                <p>This is <strong>raw, unprocessed</strong> HTML.</p>
                <!-- This is a raw HTML comment -->
            </div>
        }

        p { text { "More content from CHTL" } }
    }
}
