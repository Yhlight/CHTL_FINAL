[Import] @Chtl from "ns1.chtl";
[Import] @Chtl from "ns2.chtl";

html {
    body {
        // This should fail with an ambiguity error, because "Box" exists
        // in both "space1" and "space2".
        @Element Box;
    }
}
