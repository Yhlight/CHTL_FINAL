body {
    // Test the 'true' branch
    div {
        id: "box-blue";
        style {
            width: 100px;
            background-color: width > 50px ? "blue" : "red";
        }
    }
    // Test the 'false' branch
    span {
        id: "box-red";
        style {
            width: 25px;
            background-color: width > 50px ? "blue" : "red";
        }
    }
}
