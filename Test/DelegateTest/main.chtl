{{#parent-container}}->Delegate {
    target: {{.child-button}},
    click: (e) => { console.log('Button clicked:', e.target.textContent); }
};

{{#parent-container}}->Delegate {
    target: [{{.child-span}}],
    mouseover: (e) => { e.target.style.color = 'red'; }
};
