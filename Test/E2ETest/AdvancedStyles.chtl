// Test for advanced styles and expressions
div {
    id: "main-box";
    width: 100 * 2; // width should be 200

    style {
        // This should become an inline style
        height: 300px;

        // This should be a hoisted style rule
        .inner-text {
            font-size: 14px;
        }

        // This should use the parent's id (#main-box)
        &:hover {
            border: "1px solid black";
        }
    }

    p {
        class: "inner-text";
        text {
            Some text content.
        }
    }
}
